import { Product } from '@/types/product';

export type Props = {
  data: Product[];
};

const MarketplaceBlockes = ({ data }: Props) => {
  return <div>ToDO</div>;
};

export { MarketplaceBlockes };
